#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef unsigned long long ull;

void solve();

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t; cin >> t;
    for (int i = 0; i < t; i++) {
        int n; cin >> n;
        int a[n] = {0};
        for (int j = 0; j < n; j++) {
            cin >> a[j];
        }
        solve();
    }
    return 0;
}

void solve() {
    // code
}
